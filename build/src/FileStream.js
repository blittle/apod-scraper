var apod;
(function (apod) {
    var FileStream;
    (function (FileStream) {
        function test() {
        }
        FileStream.test = test;
    })(FileStream || (FileStream = {}));
})(apod || (apod = {}));
